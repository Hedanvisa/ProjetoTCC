<div class="container">
  <div class="list-group">
    <% @professores.each do |professor| %>
      <p href="#" class="list-group-item"><%=professor.nome%> <%= professor.email %></p>
    <% end %>
  </div>
</div>
